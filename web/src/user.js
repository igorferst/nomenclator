const login = function() {
    return Promise.resolve({})
};

const logout = function() {
    return Promise.resolve({})
}

const get = function() {
    return Promise.resolve({})
}

export {login, logout, get};
